// How long a sitting can be. Four lengths, one line, on the testable side of
// the DOM: the button that offers them is built in two toolbars now (the
// page's and the Contents'), and the whole point of one widget is that both
// offer the SAME four. A list is what makes that checkable in plain Node.
//
// Two through twenty: short enough that the first one is not a commitment,
// long enough at the top that the timer is worth having. Ascending, because
// the popover reads top to bottom.
export const SIT_MINUTES = [2, 5, 10, 20];
